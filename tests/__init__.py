import json
# import json
# import os
# from config import BASE_DIR


# This is the base test class where all global tests will be defined
class BaseTestClass:
    pass
    # def __init__(self):
    #     self.language = LANGUAGE
    #     file_path = os.path.join(BASE_DIR, 'languages', self.langage)

    #     if(not os.path.exists(file_path)):
    #         raise Exception("The language file is not present.")

    #     f = open()
    #     self.language_data = json.load(f)
    #     f.close()

    # def fetch_constant_value(self, keyName):
    #     if(not keyName in self.language_data):
    #         raise Exception(f"The constant {keyName} is not present for the language {self.language}.")
    #     return self.language[keyName]
